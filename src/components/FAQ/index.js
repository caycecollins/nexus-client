import React from 'react'
import PropTypes from 'prop-types'

import { BSContainer } from '../../wrappers/bootstrap'

function FAQ (props) {
  return (
    <BSContainer>
      <br />
      FAQ
    </BSContainer>
  )
}

FAQ.propTypes = {
}

export default FAQ
